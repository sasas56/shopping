# 电商订单模块实现文档

基于 **Spring Boot 3.2.5 + MyBatis Plus 3.5.6** 实现的订单 CRUD 接口模块。

---

## 一、实现思路

### 1.1 整体架构

采用经典三层架构，职责清晰：

```
Controller（接口层） → Service（业务层） → Mapper（持久层） → MySQL/H2
```

| 层次 | 职责 |
|------|------|
| **Controller** | 接收 HTTP 请求、参数校验、统一响应封装 |
| **Service** | 订单业务逻辑、状态流转、事务控制 |
| **Mapper** | 基于 MyBatis Plus `BaseMapper` 完成 CRUD |
| **Entity/DTO** | 数据库实体与接口传输对象分离 |

### 1.2 数据库设计

订单采用 **主表 + 明细表** 一对多结构：

- `t_order`：订单主表，存储用户、地址、总金额、状态及时间节点
- `t_order_item`：订单明细表，存储每笔订单中的商品、数量、成交单价

订单状态流转：

```
pending（待支付）→ paid（已支付）→ shipped（已发货）→ completed（已完成）
pending（待支付）→ cancelled（已取消）
```

### 1.3 技术选型要点

- **MyBatis Plus**：`BaseMapper` 提供基础 CRUD，`LambdaQueryWrapper` 构建条件查询，`PaginationInnerInterceptor` 实现分页
- **Spring Validation**：`@Valid` + `@NotNull` 等注解做入参校验
- **统一响应**：`Result<T>` 封装 `code / message / data`
- **全局异常**：`GlobalExceptionHandler` 统一处理业务异常与校验异常
- **事务**：创建订单、删除订单使用 `@Transactional` 保证主表与明细表一致性

### 1.4 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/orders` | 分页查询（支持 userId、status 筛选） |
| GET | `/api/orders/{id}` | 查询订单详情（含明细） |
| POST | `/api/orders` | 创建订单 |
| PUT | `/api/orders/{id}` | 更新订单状态 |
| DELETE | `/api/orders/{id}` | 删除订单（级联删除明细） |

### 1.5 项目结构

```
java-order-service/
├── sql/order_schema.sql              # MySQL 表结构
├── src/main/java/com/shopping/order/
│   ├── OrderApplication.java         # 启动类
│   ├── controller/OrderController.java
│   ├── service/OrderService.java
│   ├── service/impl/OrderServiceImpl.java
│   ├── mapper/OrderMapper.java
│   ├── mapper/OrderItemMapper.java
│   ├── entity/Order.java
│   ├── entity/OrderItem.java
│   ├── dto/                          # 请求/响应 DTO
│   ├── common/Result.java            # 统一响应
│   ├── common/GlobalExceptionHandler.java
│   ├── exception/BusinessException.java
│   └── config/MybatisPlusConfig.java
├── src/test/java/.../OrderControllerTest.java  # 接口集成测试
└── pom.xml
```

---

## 二、表结构 SQL

完整 SQL 文件：`sql/order_schema.sql`

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS shopping_order
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;

USE shopping_order;

-- 订单主表
CREATE TABLE IF NOT EXISTS t_order (
    id              BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '订单ID',
    user_id         BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    address_id      BIGINT UNSIGNED NOT NULL COMMENT '收货地址ID',
    total_amount    DECIMAL(10, 2)  NOT NULL DEFAULT 0.00 COMMENT '订单总金额',
    status          VARCHAR(20)     NOT NULL DEFAULT 'pending' COMMENT '订单状态',
    created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    pay_time        DATETIME        NULL COMMENT '支付时间',
    ship_time       DATETIME        NULL COMMENT '发货时间',
    complete_time   DATETIME        NULL COMMENT '完成时间',
  PRIMARY KEY (id),
  KEY idx_order_user_id (user_id),
  KEY idx_order_status (status),
  KEY idx_order_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单主表';

-- 订单明细表
CREATE TABLE IF NOT EXISTS t_order_item (
    id          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '明细ID',
    order_id    BIGINT UNSIGNED NOT NULL COMMENT '订单ID',
    product_id  BIGINT UNSIGNED NOT NULL COMMENT '商品ID',
    quantity    INT UNSIGNED    NOT NULL DEFAULT 1 COMMENT '购买数量',
    price       DECIMAL(10, 2)  NOT NULL COMMENT '成交单价',
  PRIMARY KEY (id),
  KEY idx_order_item_order_id (order_id),
  KEY idx_order_item_product_id (product_id),
  CONSTRAINT fk_order_item_order FOREIGN KEY (order_id) REFERENCES t_order (id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单明细表';
```

---

## 三、接口代码说明

### 3.1 核心 Controller

路径：`src/main/java/com/shopping/order/controller/OrderController.java`

```java
@RestController
@RequestMapping("/api/orders")
public class OrderController {
    // GET  /api/orders          - 分页列表
    // GET  /api/orders/{id}     - 订单详情
    // POST /api/orders          - 创建订单
    // PUT  /api/orders/{id}     - 更新状态
    // DELETE /api/orders/{id}   - 删除订单
}
```

### 3.2 请求/响应示例

**创建订单 POST /api/orders**

```json
{
  "userId": 1,
  "addressId": 1,
  "items": [
    { "productId": 1, "quantity": 2, "price": 29.90 },
    { "productId": 2, "quantity": 1, "price": 19.90 }
  ]
}
```

**成功响应**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "addressId": 1,
    "totalAmount": 79.70,
    "status": "pending",
    "createdAt": "2024-06-14T10:00:00",
    "items": [
      { "id": 1, "orderId": 1, "productId": 1, "quantity": 2, "price": 29.90 },
      { "id": 2, "orderId": 1, "productId": 2, "quantity": 1, "price": 19.90 }
    ]
  }
}
```

**更新状态 PUT /api/orders/{id}**

```json
{ "status": "paid" }
```

**分页查询 GET /api/orders?userId=1&status=pending&page=1&size=10**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [ /* OrderVO 列表 */ ],
    "total": 1,
    "page": 1,
    "size": 10
  }
}
```

### 3.3 关键业务逻辑（OrderServiceImpl）

- **创建**：计算 `totalAmount = Σ(price × quantity)`，插入主表后批量插入明细，默认状态 `pending`
- **更新**：校验状态流转合法性（如仅 `pending` 可支付、`paid` 可发货），并记录对应时间字段
- **删除**：先删明细再删主表，事务保证一致性

---

## 四、测试报告

### 4.1 测试环境

| 项目 | 版本/配置 |
|------|-----------|
| Java | OpenJDK 17.0.3 |
| Spring Boot | 3.2.5 |
| MyBatis Plus | 3.5.6 |
| 测试数据库 | H2 内存库（profile: test） |
| 测试框架 | JUnit 5 + MockMvc |

### 4.2 测试执行

```powershell
$env:JAVA_HOME = "C:\Program Files\ojdkbuild\java-17-openjdk-17.0.3.0.6-1"
cd "java-order-service"
mvn test
```

### 4.3 测试结果汇总

| 指标 | 结果 |
|------|------|
| 测试类 | `OrderControllerTest` |
| 测试用例数 | **9** |
| 通过 | **9** |
| 失败 | **0** |
| 错误 | **0** |
| 跳过 | **0** |
| 总耗时 | **8.118 s** |
| 结论 | **全部通过 ✅** |

### 4.4 用例明细

| # | 用例名称 | 接口 | 预期 | 结果 |
|---|----------|------|------|------|
| 1 | createOrder_shouldReturn201WithOrderData | POST /api/orders | 创建成功，status=pending，totalAmount=79.70，2条明细 | ✅ 通过 |
| 2 | getOrderById_shouldReturnOrderDetail | GET /api/orders/{id} | 返回订单及 2 条明细 | ✅ 通过 |
| 3 | listOrders_shouldReturnPagedResult | GET /api/orders | total=1，分页正常 | ✅ 通过 |
| 4 | updateOrderStatus_toPaid_shouldSucceed | PUT /api/orders/{id} | status=paid，payTime 存在 | ✅ 通过 |
| 5 | updateOrderStatus_toShipped_shouldSucceed | PUT /api/orders/{id} | status=shipped，shipTime 存在 | ✅ 通过 |
| 6 | updateOrderStatus_toCompleted_shouldSucceed | PUT /api/orders/{id} | status=completed，completeTime 存在 | ✅ 通过 |
| 7 | getOrderById_notFound_shouldReturn400 | GET /api/orders/99999 | code=404，message=订单不存在 | ✅ 通过 |
| 8 | createOrder_invalidRequest_shouldReturn400 | POST /api/orders | 缺少必填字段，code=400 | ✅ 通过 |
| 9 | deleteOrder_shouldSucceed | DELETE /api/orders/{id} | 删除成功，再次查询返回 404 | ✅ 通过 |

### 4.5 CRUD 覆盖说明

| CRUD | 对应测试用例 |
|------|--------------|
| Create | 用例 1、8（正常创建 + 参数校验） |
| Read | 用例 2、3、7（详情、列表、不存在） |
| Update | 用例 4、5、6（状态流转） |
| Delete | 用例 9 |

---

## 五、启动说明

### 环境要求

- JDK **17+**（Spring Boot 3 要求）
- Maven 3.6+
- MySQL 8.0+（生产环境）

### 配置数据库

1. 执行 `sql/order_schema.sql` 创建库表
2. 修改 `src/main/resources/application.yml` 中的数据库连接信息

### 启动服务

```powershell
$env:JAVA_HOME = "C:\Program Files\ojdkbuild\java-17-openjdk-17.0.3.0.6-1"
cd java-order-service
mvn spring-boot:run
```

服务默认端口：**8080**，接口前缀：`/api/orders`
