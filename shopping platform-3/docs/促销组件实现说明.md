# 促销组件实现说明

在商品详情页（`ProductDetail.vue` 父组件）中，商品加载完成后通过 `getProductPromotion` 组装 `ProductPromotion` 促销对象，再分别用 `:promotion="promotionInfo"` 向 `PromotionTag`（展示标签与原价）和 `CountDown`（根据 `endTime` 每秒刷新倒计时）两个子组件传值；子组件通过 `defineProps` 声明接收同一套促销数据结构，实现父传子的单向数据流，有促销时才渲染对应区域。
